# Git main branch oluşturun
git checkout -b main

# Kodu dizine yerleştirin (örneğin `app.py` için)
echo "# IAS4 Global Command Center" > README.md
touch app.py

# Dosyalarınızı ekleyin ve commit yapın
git add .
git commit -m "Initial commit"